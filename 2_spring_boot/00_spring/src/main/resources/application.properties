# Import a nonstandard filename. Its own activation condition limits it to prod.
spring.config.import=optional:classpath:app-prod.yaml

# Shared value inherited by both profiles.
logger.app-name=Learning Orders

# Shared default overridden by whichever profile is active.
logger.level=INFO
