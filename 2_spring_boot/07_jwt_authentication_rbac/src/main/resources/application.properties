spring.application.name=jwt-authentication-rbac
server.port=8080

# Demo secret only. Production secrets belong in a secret manager or environment variable.
app.jwt.secret=demo-secret-demo-secret-demo-secret-demo-secret
app.jwt.access-token-seconds=900
app.jwt.refresh-token-seconds=604800
